remember - a coord example application
======================================

remember uses [coord](https://github.com/evax/coord) to coordinate the start
of a mnesia cluster

Build using `make`, then start nodes with `start name` in different shells.

